version: "3.7"

services:
  app:
    build:
      args:
        NGINX_VERSION: ${IMAGE_BASE_NGINX}
      dockerfile: Dockerfile.production
    environment:
      - NODE_ENV=production
    ports:
      - "80:80"
    restart: always

